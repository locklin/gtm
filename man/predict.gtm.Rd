% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtm-methods.R
\name{predict.gtm}
\alias{predict.gtm}
\title{Predict Method for GTM}
\usage{
\method{predict}{gtm}(
  object,
  newdata = NULL,
  type = c("means", "modes", "responsibilities"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{"gtm"} object from \code{\link[=GtmFit]{GtmFit()}}.}

\item{newdata}{Optional numeric matrix (N_new x D). If NULL, uses the
training data stored in the model.}

\item{type}{One of \code{"means"} (R-weighted mean latent positions),
\code{"modes"} (latent position of highest-responsibility node),
or \code{"responsibilities"} (full K x N responsibility matrix).}

\item{...}{Ignored.}
}
\value{
Depends on \code{type}:
\describe{
\item{"means"}{Numeric matrix (N x L) of mean latent positions.}
\item{"modes"}{Numeric matrix (N x L) of mode latent positions.}
\item{"responsibilities"}{Numeric matrix (K x N) of responsibilities,
where K is the number of grid nodes and N is the number of data points.}
}
}
\description{
Projects data into the latent space of a fitted GTM model.
}
\examples{
data(iris)
model <- GtmFit(as.matrix(iris[, 1:4]), n_iter = 20)
proj <- predict(model, type = "means")
head(proj)

}
