% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtm-plot.R
\name{plot.gtm}
\alias{plot.gtm}
\title{Plot Method for GTM}
\usage{
\method{plot}{gtm}(
  x,
  type = c("means", "modes", "density", "umatrix", "components", "magnification",
    "convergence"),
  labels = NULL,
  property = NULL,
  component = NULL,
  main = NULL,
  palette = NULL,
  ...
)
}
\arguments{
\item{x}{A fitted \code{"gtm"} object from \code{\link[=GtmFit]{GtmFit()}}.}

\item{type}{Type of plot. One of:
\describe{
\item{\code{"means"}}{Data points projected to R-weighted mean latent positions.}
\item{\code{"modes"}}{Data points projected to highest-responsibility node.}
\item{\code{"density"}}{Heatmap of cumulative responsibility per node.}
\item{\code{"umatrix"}}{Heatmap of neighbor distances between mapped points.}
\item{\code{"components"}}{Per-variable heatmap of R-weighted data values.}
\item{\code{"magnification"}}{Heatmap of local area distortion (Jacobian).}
\item{\code{"convergence"}}{Line plot of log-likelihood over iterations.}
}}

\item{labels}{Optional factor or character vector (length N) for coloring
points in \code{"means"} and \code{"modes"} plots.}

\item{property}{Optional numeric vector (length N) for coloring points
by a continuous variable in \code{"means"} and \code{"modes"} plots.}

\item{component}{Integer or character, which variable(s) to show for
\code{"components"} type. Default shows all (one per panel).}

\item{main}{Plot title. If NULL, a default is used.}

\item{palette}{Color palette: a function(n) returning n colors, or a
character vector of colors. Default: \code{hcl.colors(64, "YlOrRd")}.}

\item{...}{Additional arguments passed to plotting functions.}
}
\description{
Produces a variety of visualizations for a fitted Generative Topographic
Map, following the \code{kohonen::plot.kohonen} pattern of \verb{type=} dispatch.
}
\examples{
data(iris)
model <- GtmFit(as.matrix(iris[, 1:4]), n_iter = 20)
plot(model, type = "convergence")

}
